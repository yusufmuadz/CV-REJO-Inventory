class HomeEntity {
  final int totalRowTransaction;
  final int totalRowTransactionHistory;

  const HomeEntity({
    required this.totalRowTransaction,
    required this.totalRowTransactionHistory,
  });
}
